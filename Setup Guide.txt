Make a project folder
npm init (followed promps, made sure index was .ts)
Make a src folder in the project
I changed the start in the package.json to .src/index.ts
Add "start": "ts-node src/index.ts" to scripts 
npm install express
npm install -D typescript ts-node @types/express.
	Commited and pushed
Create errors folder in src
Create errors in that folder
	Commited and pushed
Create models folder in src
Create models in that folder
	Commited and pushed
Create middleware folder in src
Create middleware in that folder
	Committed and pushed
Created index.ts
Created daos, routers, and utils folders in src
